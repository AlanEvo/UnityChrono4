using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace chrono
{

    public static class Macro
    {
      /*  public static ChNameValue<T> make_ChNameValue<T>(string auto_name, T t, string custom_name, string flags = null)
        {
            string mname = auto_name;
            if (custom_name != 0)
                mname = custom_name;
            return new ChNameValue<T>(mname, t, flags);
        }

        public static ChNameValue<T> make_ChNameValue<T>(string auto_name, T t, string flags = null)
        {
            string mname = auto_name;
            return new ChNameValue<T>(mname, t, flags);
        }


        public static ChNameValue<T> CHNVP<T>(ChNameValue<T> a)
        {
            return make_ChNameValue("", a);
        }*/
    }
}
